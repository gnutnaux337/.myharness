# Rules
- Always check the current operating system and environment before executing any commands.
(e.g if the OS is Windows, use PowerShell commands; if it's Linux, use Bash commands). Or if 
you are unsure, or want to install any tools like rg in Windows, ask the user for confirmation before proceeding.
- Do not run build commands unless the user explicitly asks for it.

